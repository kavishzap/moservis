/** Shared width + gutters — navbar, hero panel, and sections align to these edges. */
export const siteContainer =
  "mx-auto w-full max-w-[min(100%,84rem)] px-4 sm:px-5 lg:px-6"

/** Hero uses slightly wider edges so the panel fills the viewport. */
export const heroContainer =
  "mx-auto w-full max-w-[min(100%,92rem)] px-3 sm:px-4 lg:px-5"

/** Sticky navbar clearance for in-page `#section` links. */
export const sectionScrollMargin =
  "scroll-mt-[6.25rem] sm:scroll-mt-[7rem]"
